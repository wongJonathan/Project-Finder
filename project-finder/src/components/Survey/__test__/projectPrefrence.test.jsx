import React from 'react';
import { render } from '@testing-library/react';

import ProjectPreference from '../projectPreference';


describe('Project Preference', () => {
  it('Should render', () => {
    render(<ProjectPreference />);
  });
});
